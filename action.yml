name: 'Conda Tools'
description: 'Build and publish conda packages'
author: 'fdiblen'
branding:
  icon: 'package'
  color: 'green'
inputs:
  envFileName:  # id of input
    description: 'Envirenment file name'
    required: true
    default: 'environment.yml'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.envFileName }}
    - 'foo'
    - 'bar'